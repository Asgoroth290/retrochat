import { FC, ReactElement } from "react";
import { Message } from "../Model/Message";

type MessageComponentProps = {
  message: Message;
}
export const MessageComponent: FC<MessageComponentProps> = (props: MessageComponentProps) => {
  return {
    <
  }
}